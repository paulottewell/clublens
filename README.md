# clublens
ClubLens — offline browser tool for analysing clubs participation across pupil priority groups. Reads a Bromcom .xlsx export; all processing runs locally — no data leaves the device. Built for primary schools within a Multi-Academy Trust. Single self-contained HTML file, no install required.
